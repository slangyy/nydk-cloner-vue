# 工艺系统页面规格

- 参考路径：`/technique.html?p=2`
- Hero：`e2098136-e0c0-4215-a40e-b46490d10b79.png`，
  `PROCESS SYSTEM / 工艺系统`。
- 正文使用 1440px 容器，上下各 150px 留白。
- 两组图文均为 `520px 图片 + 820px 文案`，垂直居中并左右交错：
  - “国际视野、匠心独运”：图片在左、文案在右，组后间距 100px。
  - “领军实力、极致工艺”：文案在左且右对齐、图片在右，组后间距 150px。
- 标题为 48px、700 字重、80px 行高；标题下方为 120 × 2px 玫瑰色
  分隔线。正文为 18px、`#777`、47.88px 行高。
- 两张竖图按原始 `520 × 660` 比例完整显示；底图
  `fdf04945-c6b4-4d62-8f4a-37a2fd2ade43.jpg` 紧随第二组，按
  `1440 × 810` 完整显示。
- 进入动画：第一组图片从左、文案从右；第二组方向相反；底图向上淡入。
- `≤998px` 改为图片在上、文字在下的单列布局，并保持相同内容顺序。
